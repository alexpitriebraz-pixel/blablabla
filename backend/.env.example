# Servidor
PORT=3000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/blablabla

# Origin do app (CORS) — separadas por vírgula. Incluir capacitor://localhost e null para apps móveis
CLIENT_ORIGIN=http://localhost:3000,http://localhost,capacitor://localhost,null

# Firebase — Service Account (gerado no Firebase Console > Configurações > Conta de serviço)
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# Spotify — App Credentials (gerado em developer.spotify.com)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
